hrs = input('Enter Hours:')
rte = input ('Enter rate per hour:')
h = float(hrs)
r = float(rte)
if h > 40:
    pay = h * r
    newp = (h - 40.0) * (r * 0.5)
    finalpay = pay + newp
    print (finalpay)
else:
    finalpay = h * r
    print (finalpay)
